{% extends "FOSUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
    <h3>Регистрация</h3>
    <!--p>
        связывает людей, которым надо что-то и куда-то перевезти с водителями, перевозчиками и туристами, которые в любом случае едут в этом направлении.
    </p-->
    <form class="fos_user_registration_register m-t" role="form" action="{{ path("fos_user_registration_register") }}" {{ form_enctype(form) }} method="post">
        {{ form_errors(form) }}
        {{ form_errors(form.username) }}

        <div class="form-group input-group ">
            <span class="input-group-addon">+7</span>
            {{ form_widget(form.username, { 'attr': {'placeholder': 'Номер телефона', 'class': 'form-control'} }) }}
            <span class="input-group-addon">10 цифр</span>
        </div>

        {{ form_rest(form) }}

        <p class="text-muted text-center"><small>Нажимая кнопку "Зарегистрироваться", я принимаю <a href="{{ path('binidini_rules') }}"  target="_blank">условия оферты</a></small></p>

        <button type="submit" id="_submit" name="_submit" class="btn btn-primary block full-width m-b">Зарегистрироваться</button>
        <p class="text-muted text-center"><small>Уже с нами?</small></p>
        <a class="btn btn-sm btn-success btn-block" href="{{ path("fos_user_security_login") }}">Вход в Титимити</a>
    </form>
{% endblock fos_user_content %}
