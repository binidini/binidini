{% import 'BinidiniWebBundle:Frontend/Macros:page.html.twig' as page %}

{% extends 'BinidiniWebBundle::layout.html.twig' %}

{% block pageheader %}Активные перевозки{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-sm-12">
            <div class="wrapper wrapper-content animated fadeInRight">
                <div class="ibox float-e-margins">

                    <div class="ibox-title">
                        {% if shippings.nbresults > 0 %}
                            {{ page.paging(shippings) }}

                            <div class="ibox-tools">

                                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                                    по {{ shippings.maxperpage() }} записей
                                </a>
                                {{ include('BinidiniWebBundle::Frontend/Shipping/inc/_paginate_filter.html.twig', {'path': 'binidini_core_carrier_active_index'}) }}

                            </div>
                        {% else %}заказов нет{% endif %}
                    </div>
                    <div class="ibox-content">

                        {% if shippings.nbresults > 0 %}

                            {{ include('BinidiniWebBundle::Frontend/Shipping/inc/_shippings_list.html.twig', {'shippings': shippings}) }}

                            <div class="row">
                                <div class="col-sm-6">
                                </div>

                                <div class="col-sm-6 text-right">
                                    {{ page.pagination(shippings) }}
                                </div>
                            </div>

                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script type="text/javascript">

        $(document).ready(function() {

            $('.contact-box').each(function() {
                animationHover(this, 'pulse');
            });

        });

    </script>
{% endblock %}