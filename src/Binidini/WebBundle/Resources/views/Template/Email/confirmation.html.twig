{% extends 'BinidiniWebBundle::Template/layout.html.twig' %}

{% block content_header %}
    <td style="font-size: 16px;
    color: #fff;
    font-weight: 500;
    padding: 20px;
    text-align: center;
    border-radius: 3px 3px 0 0;
    background: #1ab394;">
        Подтверждение электронного адреса на {{ "binidini.site.name" | trans }}
    </td>
{% endblock %}

{% block content_body %}
    <tr>
        <td style="padding: 0 0 20px;">
            Ваш электронный адрес был зарегистрирован на сайте <a href="{{ url('binidini_home') }}">
                {{ "binidini.site.domain" | trans }}
            </a>
        </td>
    </tr>
    <tr>
        <td style="padding: 0 0 20px;">
            Для подтверждения электронного адреса нажмите на кнопку "Подтвердить"
        </td>
    </tr>
    <tr>
        <td style="padding: 0 0 20px;text-align: center;">
            <a href="{{ url('binidini_email_confirm', {token: user.confirmationToken}) }}" style="text-decoration: none;
        color: #FFF;
        background-color: #1ab394;
        border: solid #1ab394;
        border-width: 5px 10px;
        line-height: 2;
        font-weight: bold;
        text-align: center;
        cursor: pointer;
        display: inline-block;
        border-radius: 5px;
        text-transform: capitalize;">Подтвердить</a>
        </td>
    </tr>
{% endblock %}

{% block footer %}

{% endblock %}
