{% extends 'BinidiniWebBundle::Template/layout.html.twig' %}

{% block content_header %}
    <td class="alert alert-good" style="font-size: 16px;
    color: #fff;
    font-weight: 500;
    padding: 20px;
    text-align: center;
    border-radius: 3px 3px 0 0;
    background: #1ab394;">
        Подтверждение почты на titimiti.ru
    </td>
{% endblock %}

{% block content_body %}
    <tr>
        <td class="content-block" style="padding: 0 0 20px;">
            Ваша почта была зарегестрирована на сайте <a href="{{ url('binidini_home') }}">titimiti.ru</a>
        </td>
    </tr>
    <tr>
        <td class="content-block" style="padding: 0 0 20px;">
            Для подтверждения электронной почты перейдите по ссылке
        </td>
    </tr>
    <tr>
        <td class="content-block aligncenter" style="padding: 0 0 20px;text-align: center;">
            <a href="{{ url('binidini_email_confirm', {token: user.confirmationToken}) }}" class="btn-primary" style="text-decoration: none;
        color: #FFF;
        background-color: #1ab394;
        border: solid #1ab394;
        border-width: 5px 10px;
        line-height: 2;
        font-weight: bold;
        text-align: center;
        cursor: pointer;
        display: inline-block;
        border-radius: 5px;
        text-transform: capitalize;">Подтвердить</a>
        </td>
    </tr>
{% endblock %}

